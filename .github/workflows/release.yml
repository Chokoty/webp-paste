name: release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Require Apple Silicon runner
        run: test "$(uname -m)" = "arm64"
      - name: Install libwebp
        run: brew install webp
      - name: Build and convert fixture
        run: ./scripts/check.sh
      - name: Zip .app
        run: ditto -c -k --keepParent mac/dist/webp-paste.app webp-paste-macos-arm64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: macos
          path: webp-paste-macos-arm64.zip
          if-no-files-found: error

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      - name: Publish
        shell: pwsh
        run: ./win/build.ps1
      - name: Convert fixture
        shell: pwsh
        run: |
          $out = Join-Path $env:TEMP "out.webp"
          & ./win/dist/webp-paste.exe --convert ./fixtures/screenshot.png $out
          if (-not (Test-Path $out)) { throw "convert produced no file (exit $LASTEXITCODE)" }
          $bytes = [IO.File]::ReadAllBytes($out)
          $riff = [Text.Encoding]::ASCII.GetString($bytes[0..3])
          $tag = [Text.Encoding]::ASCII.GetString($bytes[8..11])
          if ($riff -ne "RIFF" -or $tag -ne "WEBP") { throw "not webp: $riff $tag" }
          $src = (Get-Item ./fixtures/screenshot.png).Length
          if ($bytes.Length -ge $src) { throw "not smaller: $($bytes.Length) >= $src" }
          Write-Host "ok webp $($bytes.Length) from $src"
      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: win/dist/webp-paste.exe
          if-no-files-found: error

  publish:
    needs: [mac, windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
      - name: Pack assets
        run: |
          set -euo pipefail
          mkdir -p out
          cp dist/macos/webp-paste-macos-arm64.zip out/
          cp dist/windows/webp-paste.exe out/webp-paste-windows-x64.exe
          cd out
          sha256sum webp-paste-macos-arm64.zip webp-paste-windows-x64.exe | tee checksums.txt
          ls -lh
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          MAC_SHA=$(cut -d' ' -f1 out/checksums.txt | sed -n '1p')
          WIN_SHA=$(cut -d' ' -f1 out/checksums.txt | sed -n '2p')
          cat > notes.md <<EOF
          Copy a screenshot. Paste a small WebP file. Menu bar on Mac, tray on Windows. The image never leaves the machine.

          ## Downloads

          | Platform | File |
          |---|---|
          | macOS (Apple Silicon) | [webp-paste-macos-arm64.zip](https://github.com/${{ github.repository }}/releases/download/${TAG}/webp-paste-macos-arm64.zip) |
          | Windows (x64) | [webp-paste-windows-x64.exe](https://github.com/${{ github.repository }}/releases/download/${TAG}/webp-paste-windows-x64.exe) |

          macOS is unsigned: first launch is **right-click → Open**. Windows is unsigned: SmartScreen may warn. The Windows build is self-contained (.NET 8 bundled).

          ## SHA-256

          \`\`\`
          ${MAC_SHA}  webp-paste-macos-arm64.zip
          ${WIN_SHA}  webp-paste-windows-x64.exe
          \`\`\`
          EOF
          if gh release view "$TAG" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            gh release upload "$TAG" \
              out/webp-paste-macos-arm64.zip \
              out/webp-paste-windows-x64.exe \
              --repo "${{ github.repository }}" \
              --clobber
            gh release edit "$TAG" \
              --repo "${{ github.repository }}" \
              --notes-file notes.md
          else
            gh release create "$TAG" \
              --repo "${{ github.repository }}" \
              --title "$TAG" \
              --notes-file notes.md \
              --generate-notes \
              out/webp-paste-macos-arm64.zip \
              out/webp-paste-windows-x64.exe
          fi
